---

services:
  homeassistant:
    image: ghcr.io/home-assistant/home-assistant:2026.4.3
    container_name: homeassistant
    restart: always
    volumes:
      - ${COMPOSE_ROOT_PATH}/homeassistant:/config:z
